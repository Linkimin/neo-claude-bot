# Заполни в .env (который в .gitignore). Для M0 обычно ничего не нужно,
# если Claude Code на машине уже залогинен. Ключ — только если SDK потребует.
# ANTHROPIC_API_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_USER_ID=
